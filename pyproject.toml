[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "business-workflow-automation-demo"
version = "0.1.0"
description = "Reliable event-driven business workflow automation demo."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Dmytro Lymar"}]
[tool.setuptools.packages.find]
where = ["src"]
